/**
 * What the host blob says (Brand.md §9). It lived in `scene.tsx` while the portfolio was a canvas; the canvas
 * went on 2026-09-16 and these two lines outlived it, because the blob did.
 *
 * The second one is the honest answer to anything typed at the guided chat that the keyword table cannot place.
 * It says why it cannot answer rather than failing silently, which is the rule for every empty state here.
 */
export const HOST_SAYS = "What are we doing today?";
export const HOST_CANT_CHAT =
  "I can't chat yet — the model that will live in this blob is still being trained. Pick one of these for now.";
