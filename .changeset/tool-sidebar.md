---
"@no-origins/ui": minor
---

`ToolScreen` gains the editor's two regions (Admin.md §6.5 row 1): a left `sidebar` for the palette and the outline — glass-1, sticky, the inspector's mirror at the Menu column's width — and a `flush` mode that pins the screen to the viewport and strips main's gutters so the canvas fills it edge to edge. Below `lg` the sidebar stacks above main.
