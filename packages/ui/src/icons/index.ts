/**
 * @no-origins/ui/icons — the Icon atom (Atomic.md D8). Needs the optional peer `@phosphor-icons/react`; a host that
 * never imports this entry never loads it.
 */
export { Icon, iconNames } from "./Icon";
export type { IconProps, IconName } from "./Icon";
