# Raw Source Documents

Place all immutable raw source materials here (PDFs, transcripts, markdown articles, project specifications). 

AI agents will ingest these documents and compile/integrate their findings into the structured files under the `wiki/` directory.

Do not edit files inside this directory once added; treat them as the immutable source of truth.
